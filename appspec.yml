version: 0.0
os: linux
files:
  - source: ./
    destination: /home/ec2-user
hooks:
  BeforeInstall:
   - location: install_dependencies.sh
     timeout: 300
     runas: ubuntu
  ApplicationStop: 
     timeout: 10
     runas: ubuntu
